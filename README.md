# samm

New version of the SAMM application, developed entirely in VueJS inside a single page application and consuming data from a Rest API. For this project I also used HTML, CSS (Bootstrap and SASS preprocessor) and Vuetify.
